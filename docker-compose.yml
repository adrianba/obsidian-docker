services:
  obsidian-sync:
    image: ghcr.io/adrianba/obsidian-docker:latest
    restart: unless-stopped
    user: "1000:1000"
    volumes:
      - ./vault:/vault
      - obsidian-config:/config/obsidian-headless

volumes:
  obsidian-config:
